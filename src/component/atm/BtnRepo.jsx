const BtnRepo = ({repositorio}) => {
  return(
    <a href={repositorio} className="py-1 px-2 rounded border border-orange-400" target="_blank">
        Ver el repositorio
    </a>
  )
}

export default BtnRepo